# 1. מאיזה בסיס אנחנו מתחילים? (מערכת הפעלה לינוקס שכבר יש בה Node.js)
FROM node:18-alpine

# 2. איפה בתוך הקונטיינר נשים את הקבצים שלנו?
WORKDIR /app

# 3. העתקת הקובץ שלנו מהמחשב לתוך הקונטיינר
COPY server.js .

# 4. איזה פורט הקונטיינר הזה חושף החוצה?
EXPOSE 8080

# 5. מה הפקודה שתרוץ כשמישהו יפעיל את הקונטיינר?
CMD ["node", "server.js"]